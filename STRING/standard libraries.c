# include<stdio.h>
int main(){
    /*1} strlen(string name)-> this is the inbuilt function use to calculate the length of the string.

    2} strcpy(string name)-> it is use to copy paste one string to another string .
    ex: char str1 = "hello ";
    char str2 = "world";
    strcpy(str1, str2);
    puts(str1);   -> output will be world



    3}strcat(string name)-> it is use to join two string.
    ex: char str1 = "hello ";
    char str2 = "world";
     strcat(str1, str2);
    puts(str1);   -> output will be hello world


    4}strcmp(string name)-> it is used to compare two strings and on that basis it gives output of as zero positive value , negative value .

     ex: char str1 = "hello ";
    char str2 = "world";
     
    printf("%d", strcmp(str1,str2)); -> output will be -1

    
    
    
    
    
    
    
    
    */
    return 0;
}